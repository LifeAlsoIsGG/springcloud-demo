server:
  port: 7002

spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: replica2
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://replica1:7001/eureka/

info:
  remark: 这是第二个eureka
  author: God

